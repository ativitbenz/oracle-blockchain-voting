
# Server Configuration
PORT=10000
NODE_ENV=production

# Oracle Database Configuration
DB_USER=adw_oac
DB_PASSWORD=Welcome#1234
DB_CONNECTION_STRING=demo_high

# Oracle Cloud Wallet Configuration
USE_WALLET=true
WALLET_LOCATION=/app/wallet
WALLET_PASSWORD=welcome1

# Oracle Instant Client Path
ORACLE_INSTANT_CLIENT_PATH=/opt/oracle/instantclient

# Connection Pool Configuration
DB_POOL_MIN=5
DB_POOL_MAX=50
DB_POOL_INCREMENT=5
DB_QUEUE_TIMEOUT=60000
DB_QUEUE_MAX=1000

# CORS Configuration
CORS_ORIGIN=https://0b8141c4.voting-oracle-blockchain-table.pages.dev
CORS_ALLOWED_ORIGINS=https://0b8141c4.voting-oracle-blockchain-table.pages.dev

# Table Names
BLOCKCHAIN_TABLE=votes_blockchain_v3
IMMUTABLE_TABLE=polls_immutable_v2
OPTIONS_TABLE=poll_options_v2
RECIPIENTS_TABLE=poll_recipients_v2

# Frontend URL
FRONTEND_URL=https://0b8141c4.voting-oracle-blockchain-table.pages.dev

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=benzativit@gmail.com
SMTP_PASS=lduy feqq tlxz ybfs

# Email Timeouts
SMTP_CONNECTION_TIMEOUT=10000
SMTP_GREETING_TIMEOUT=5000
SMTP_SOCKET_TIMEOUT=10000

# Email Pool Configuration
SMTP_MAX_CONNECTIONS=5
SMTP_MAX_MESSAGES=100

# Performance Configuration
SLOW_QUERY_THRESHOLD=5000

# ======================================================================
# สรุป: รวมทั้งหมด 28 Environment Variables
# ======================================================================
#
# ✅ ไม่ต้องใช้ WALLET_* Base64 variables (8 ตัว) อีกต่อไป!
# ✅ Wallet folder ถูก copy เข้า Docker image ตรงๆ แล้ว
# ✅ ง่ายกว่าและปลอดภัยกว่า
#
# ======================================================================
# วิธีใช้:
# ======================================================================
#
# 1. ไปที่ Render Dashboard > Service > Settings
# 2. เลื่อนไปที่ Environment Variables
# 3. คลิก "Add from .env" หรือ "Raw Editor"
# 4. Copy ข้อความด้านบน (เฉพาะ KEY=VALUE ไม่เอา comment)
# 5. Paste ลงไป
# 6. Save
# 7. Render จะ redeploy อัตโนมัติ
#
# ======================================================================
